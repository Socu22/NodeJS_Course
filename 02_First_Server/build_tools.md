maven, gradle, ant, npm, CMake, pip, NuGet, Rake

# Maven--------------npm

pom.xml-------------package.json
meta ----------------meta
dependencies--------dependencies
life cycles---------scripts


npm: 
npm install. after creating package.json
    node modules: is build made for your device, & mostly redundant to push and share when npm install is just one line. Then you have a node_modules automatic configured, included in this is the dependencies in package.json